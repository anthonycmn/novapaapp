# PROGRESS.md

## Phase 0 — Foundation ✅ (2026-07-26)

Built and verified:
- Next.js 15 (App Router, TS strict) + Tailwind v4 + shadcn-style component kit, hand-rolled scaffold
- Warm theatrical design tokens (curtain red / marquee gold / cream) with full dark mode and pre-paint theme script
- PWA shell: manifest route, hand-rolled service worker (offline fallback, asset caching, push handlers staked out), offline page, add-to-home-screen prompt (incl. iOS hint), placeholder icons generated by `scripts/generate-icons.mjs`
- `lib/api/` typed data layer: `DataProvider` interface, mock adapter with realistic seed (3 families, 5 students, 4 staff, 2 productions), Supabase mode switch
- `lib/platform/` adapters: push/service-worker, external links
- Auth: cookie session, sign-in/out, role model (`parent|student|staff|admin|super_admin`), demo accounts on login page
- Supabase migration `0001_foundation.sql`: profiles, families, guardians, students, hopes + RLS policies (deny-by-default, family isolation, staff-only columns via view)
- Tests: 18 access-control tests proving cross-family reads/writes are blocked and hopes privacy holds (mock mirrors RLS)
- CI: GitHub Actions typecheck+lint+test+build

Verification status:
- ✅ `npm run typecheck` clean · ✅ 17→18 tests green · ✅ `npm run build` (13 routes)
- ⚠️ Vercel deploy + real-device install: **blocked on Vercel account + Supabase project** (NEEDS-FROM-TONY.md #1, #7). App is fully demo-able locally in mock mode.

Notes:
- Local Supabase (Docker) unavailable on this machine → mock adapter enforces
  the same rules as RLS so authorization is tested continuously; SQL policies
  ship in migrations for `supabase db push`.

## Phase 1 — Profiles & Data Core (in progress)
