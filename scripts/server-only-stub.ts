// Empty stand-in for the "server-only" guard package, used ONLY by
// scripts/tsconfig.json so adapter integration scripts can run under
// plain Node. The Next.js build resolves the real package and keeps the
// client-bundle protection.
export {};
